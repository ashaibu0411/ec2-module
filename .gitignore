# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*
*.terraform.lock.hcl

# Crash Log files
crash.Log
crash.*.Log

# Exclude all .tfvars files, which are likely
# password, private keys, and other secrets
# control as they are data points which are potent
# to change depending on the environment
*.tfvars
*.tfvars.json

# ignore override files as they are usually used 
# are not checked in
override.tfstate
override.tf.json
*_override.tf
*_override.tf.json







# ignore CLI configuration files
.terraformrc
terraform.rc